# Deployment Guide

Status: Planned

This guide will describe supported deployment paths for Conductor.

Initial deployment targets are defined in [technical.md](technical.md): local developer execution, self-contained release bundles, and a single-host Docker image. Concrete commands, configuration values, backup steps, and operational checks will be added as the host application and release workflow are implemented.
